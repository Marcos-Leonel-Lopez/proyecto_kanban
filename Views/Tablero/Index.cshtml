@{
    ViewData["Title"] = "Gestión de Tableros";
    var rolUsuario = ViewData["rolUsuario"]?.ToString();
}
<section class="container mt-5">
            @await Component.InvokeAsync("MenuTablero", new { rolUsuario = rolUsuario })
            <a asp-controller="Home" asp-action="Index" class="btn btn-secondary w-100 mt-2">Volver</a>
        </div>
    </div>
</section>
